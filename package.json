{
  "name": "flowboard",
  "private": true,
  "version": "1.1.0",
  "description": "Local-first Flowboard with an adapter-ready authenticated workspace boundary",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/state-core.test.js tests/local-workspace-adapter.test.mjs",
    "check": "node --check state-core.js && node --check app.js && node --input-type=module --check < src/config.js && node --input-type=module --check < src/adapters/adapter-contract.js && node --input-type=module --check < src/adapters/local-workspace-adapter.js && node --input-type=module --check < src/main.js && node scripts/validate-static.mjs && node scripts/validate-performance.mjs",
    "validate": "npm test && npm run check && npm run build"
  },
  "devDependencies": {
    "vite": "8.2.0"
  }
}

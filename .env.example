# Public browser configuration only. Do not add service-role, Google client-secret,
# SMTP, invitation-signing, or other privileged values to Vite environment files.
# Phase A intentionally leaves these blank: no cloud sign-in or synchronization exists yet.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

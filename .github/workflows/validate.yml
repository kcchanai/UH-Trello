name: Validate Flowboard

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package.json
      - name: Unit, syntax, static, and asset-budget checks
        run: |
          npm test
          npm run check
      - name: Install ephemeral browser test tools
        run: |
          npm install --no-save --ignore-scripts @playwright/test@1.55.0 lighthouse@12.8.2
          npx playwright install --with-deps chromium
      - name: Start static site
        run: |
          node scripts/static-server.mjs > server.log 2>&1 &
          for i in {1..20}; do curl --fail --silent http://127.0.0.1:4173/ && exit 0; sleep 1; done
          cat server.log
          exit 1
      - name: Browser critical-workflow checks
        run: npx playwright test tests/browser-smoke.spec.mjs
      - name: Automated accessibility check
        run: |
          npx lighthouse http://127.0.0.1:4173/ --only-categories=accessibility --chrome-flags='--headless --no-sandbox' --output=json --output-path=lighthouse-report.json
          node scripts/assert-lighthouse.mjs
      - name: Archive diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: validation-diagnostics
          path: |
            server.log
            lighthouse-report.json
            test-results/
          if-no-files-found: ignore
